# Tips to add

* Array `find` method
* Array `filter` method
* About variable `Scope`
* What is a `Closure`
* Event firing when DOM loaded
* Intro to `ES6`
    * `Template` strings
    * `Arrow` functions
* Factory functions
